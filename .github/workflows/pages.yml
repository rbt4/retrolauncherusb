name: Build and deploy Plugcade
on:
  push:
    branches: [main]
    paths: ["launcher/**", "web/**", ".github/workflows/pages.yml"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate launcher source
        run: |
          node - <<'NODE'
          const fs = require("fs");
          const html = fs.readFileSync("launcher/Plugcade.hta", "utf8");
          const script = html.match(/<script language="JScript">([\s\S]*?)<\/script>/i);
          if (!script) throw new Error("Plugcade JScript block is missing");
          new Function(script[1]);
          [
            "launcher/START_PLUGCADE.bat",
            "launcher/PLUGCADE-LITE.bat",
            "launcher/README.txt",
            "launcher/PLATFORMS.txt",
            "launcher/Config/ai-helper.example.ini"
          ].forEach((path) => {
            if (!fs.existsSync(path)) throw new Error(path + " is missing");
          });
          const platformCount = (script[1].match(/P\("/g) || []).length;
          if (platformCount < 26) throw new Error("Expected at least 26 platform adapters");
          console.log("Launcher source passed validation with " + platformCount + " adapters.");
          NODE

      - name: Build portable Core download
        run: |
          python3 - <<'PY'
          from pathlib import Path
          for path in Path("launcher").rglob("*"):
              if path.is_file() and path.suffix.lower() in {".bat", ".hta", ".txt", ".ini"}:
                  data = path.read_bytes().replace(b"\r\n", b"\n").replace(b"\n", b"\r\n")
                  path.write_bytes(data)
          PY
          mkdir -p web/downloads
          (cd launcher && zip -qr -FS ../web/downloads/Plugcade-Core-0.3-alpha.zip .)
          (cd web/downloads && sha256sum Plugcade-Core-0.3-alpha.zip > Plugcade-Core-0.3-alpha.sha256.txt)

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
