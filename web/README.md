# Plugcade Web

The public, framework-free Plugcade launcher experience. It is deployed automatically to GitHub Pages whenever files in this folder are updated on `main`.

## Local preview

Open `index.html` in a browser. No build step or package installation is required.

## Deployment

`.github/workflows/pages.yml` publishes this directory through GitHub Pages on every push to `main`.
