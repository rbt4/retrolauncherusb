; PLUGCADE OPTIONAL AI HELPER - DESIGN TEMPLATE ONLY
; The 0.3 Core launcher does not send API requests or capture the screen.
enabled=0
provider=gemini
endpoint=http://127.0.0.1:8765/plugcade-help
capture_mode=manual_preview_only
retain_captures=0
spoken_answers=1
grownup_consent_required=1
; Never put an API key in this file or inside the launcher.
; A future Standard-edition helper must call a local/backend proxy.
